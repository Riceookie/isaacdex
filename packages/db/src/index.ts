// packages/db — warstwa dostępu do danych.
//
// Tu w ZADANIU 3 ("Dane: PostgreSQL + Prisma") zamieszka:
//  - schema.prisma (encje ze specyfikacji: Danie, Skladnik, Zamowienie, ...),
//  - migracje,
//  - eksport typowanego klienta Prisma (np. `export const prisma = new PrismaClient()`).
//
// Na razie placeholder, żeby monorepo miało komplet paczek i budowało się w całości.
export const DB_PLACEHOLDER = 'Prisma dojdzie w zadaniu 3' as const
