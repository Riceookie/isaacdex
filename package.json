{
  "name": "pizzaflow",
  "version": "0.0.0",
  "private": true,
  "packageManager": "pnpm@11.9.0",
  "description": "PizzaFlow — system zamówień i kuchni dla pizzerii (monorepo)",
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "typecheck": "turbo run typecheck",
    "start:api": "pnpm --filter @pizzaflow/api start"
  },
  "devDependencies": {
    "turbo": "^2.3.0",
    "typescript": "^5.6.0",
    "tsx": "^4.19.0"
  }
}
